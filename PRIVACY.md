# Privacy Policy

图透镜 Image Lens 是一个自备 API Key 模式的 Chrome 插件。项目作者不会提供 API Key，也不会运营用于收集用户数据的服务器。

## 本地保存的数据

插件会使用 `chrome.storage.local` 在当前浏览器本地保存以下配置：

- 用户填写的 Gemini API Key
- 用户填写的模型名称
- 是否开启自动识别
- 是否开启生图功能
- 默认生图比例和出图张数

这些配置不会写入代码仓库，也不会发送给项目作者。

## 发送到 Gemini API 的数据

当用户主动点击网页图片上的识别按钮时，插件会读取该图片，并将图片数据、页面地址和图片 alt 文本发送到用户配置的 Gemini API，用于生成提示词。

当用户主动点击「立刻生图」时，插件会将当前提示词、生图比例和出图张数发送到用户配置的 Gemini API，用于生成图片。

Gemini API 对这些数据的处理方式受 Google Gemini API 服务条款和隐私政策约束。

## 不收集的数据

项目作者不会收集、存储或分析以下数据：

- 用户的 API Key
- 用户浏览的网页内容
- 用户识别的图片内容
- 用户生成或编辑的提示词
- 用户生成的图片结果

## 权限说明

- `storage`：用于在本地保存插件配置。
- `tabs`：用于打开插件选项页或结果查看页。
- `<all_urls>`：用于在网页图片上注入识别按钮，并在用户主动点击后读取对应图片。

插件只会在用户主动触发识别或生图时调用外部 API。
