# Privacy Policy

图透镜 Image Lens 是一个自备 API Key 模式的 Chrome 插件。项目作者不会提供 API Key，也不会运营用于收集用户数据的服务器。

## 本地保存的数据

插件会使用 `chrome.storage.local` 在当前浏览器本地保存以下配置：

- 用户填写的 API Key
- 用户填写的模型名称
- 用户填写的 Base URL
- 是否开启自动识别
- 是否开启中文提示词识别
- 是否开启生图功能
- 默认生图比例和出图张数

这些配置不会写入代码仓库，也不会发送给项目作者。

## 发送到外部 API 的数据

当用户主动点击网页图片上的识别按钮时，插件会优先直接读取该图片，并将图片数据、页面地址和图片 alt 文本发送到用户当前配置的识图服务商，用于生成提示词。

如果目标网站限制了图片直接读取，插件会在本地对当前可见标签页进行一次截图，并仅裁剪出用户刚刚点击的图片区域；发送到外部 API 的是裁剪后的图片片段，不是整页截图。

当用户主动点击「立刻生图」时，插件会将当前提示词、生图比例和出图张数发送到用户当前配置的生图服务商，用于生成图片。

外部 API 对这些数据的处理方式，受对应服务商自己的服务条款和隐私政策约束。

## 不收集的数据

项目作者不会收集、存储或分析以下数据：

- 用户的 API Key
- 用户浏览的网页内容
- 用户识别的图片内容
- 用户生成或编辑的提示词
- 用户生成的图片结果

## 权限说明

- `storage`：用于在本地保存插件配置。
- `<all_urls>`：用于在网页图片上注入识别按钮，在用户主动点击后读取对应图片，并在个别网站限制直接读取图片时，本地裁剪当前可见图片区域用于识别。
- `https://generativelanguage.googleapis.com/*`：用于将用户主动触发的 Gemini 请求发送到 Gemini API。

插件只会在用户主动触发识别或生图时调用外部 API。
