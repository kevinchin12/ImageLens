# Privacy Policy

图透镜 Image Lens 是一个自备 API Key 模式的 Chrome 插件。项目作者不会提供 API Key，也不会运营用于收集用户数据的服务器。

## 本地保存的数据

插件会使用 `chrome.storage.local` 在当前浏览器本地保存以下配置：

- 用户填写的识图服务商配置
- 用户填写的生图服务商配置
- API Key
- Base URL
- 模型名称
- 是否开启自动识别
- 是否开启生图功能
- 默认生图比例和出图张数

这些配置不会写入代码仓库，也不会发送给项目作者。

## 发送到外部 API 的数据

当用户主动点击网页图片上的识别按钮时，插件会读取该图片，并将图片数据、页面地址和图片 alt 文本发送到用户当前配置的识图服务商，用于生成提示词。

当用户主动点击「立刻生图」时，插件会将当前提示词、生图比例和出图张数发送到用户当前配置的生图服务商，用于生成图片。

外部 API 对这些数据的处理方式，受对应服务商自己的服务条款和隐私政策约束。

## 取图兼容逻辑说明

为了提高在复杂网站上的识图成功率，插件可能会按以下顺序尝试获取图片数据：

1. 直接读取页面内可访问的图片数据
2. 使用页面上下文请求图片资源
3. 在必要时对当前可见页面截图，并裁出用户当前点击的图片区域

这些操作都只发生在用户主动点击识图按钮之后。

## 不收集的数据

项目作者不会收集、存储或分析以下数据：

- 用户的 API Key
- 用户浏览的网页内容
- 用户识别的图片内容
- 用户生成或编辑的提示词
- 用户生成的图片结果

## 权限说明

- `storage`：用于在本地保存插件配置。
- `tabs`：用于打开插件选项页或结果查看页，以及在必要时处理当前标签页截图兜底。
- `<all_urls>`：用于在网页图片上注入识别按钮，并在用户主动点击后读取对应图片。

插件只会在用户主动触发识图或生图时调用外部 API。
